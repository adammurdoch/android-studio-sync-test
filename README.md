A mockup of Android studio sync using the Gradle tooling API, for debugging and performance fixes. 

To install:

    ./gradlew installDist
    
To run:

    ./build/install/sync-test/bin/sync-test --project-dir <project-to-sync>
